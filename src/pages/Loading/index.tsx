import React from 'react';
import Typography from '@material-ui/core/Typography';
import Loading from 'components/Loading';

const LoadingPage = () => (
  <div>
    <Typography variant="h5">Loading Component</Typography>
    <Loading />
  </div>
);
export default LoadingPage;
